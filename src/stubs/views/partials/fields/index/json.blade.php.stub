                @if({MODEL_VARIABLE})
                    <span class="no-btn btn btn-xs btn-info">JSON</span>
                @else
                    <span class="no-btn btn btn-xs btn-danger">empty</span>
                @endif
